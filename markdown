1. md是书写格式，HTML是显示格式。

2. 在md中添加HTML只需要正常使用HTML区块标签，不过要求这个区段前后各有一个空行，并且开始和结束标签不能用tab和空格来缩进。

3. 第2点适用于在md中添加HTML的区块标签，但是可以在md中任意添加HTML的行内标签。总结来说，HTML的区块元素中插入的md语法将不被处理，在行内元素中插入是有效的。

4. md的段落，前后各有一个空行，由一个或者多个连续的文本行组成。段内可以强制换行，并且不会转换为<br />，如果要输入<br />，则需要在插入出输入两个或以上空格然后换行。
